name: Pages Deployment Ownership

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Deployment ownership
        run: |
          echo "The auto-ai-blog-business Cloudflare Pages project is connected directly to GitHub."
          echo "Pushes to main are deployed by Cloudflare Pages; GitHub Actions performs validation only."
